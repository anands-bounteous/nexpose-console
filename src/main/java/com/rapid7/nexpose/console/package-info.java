/**
 * Shared library for the Nexpose Security Console: domain model, custom
 * exceptions, report engine, mock scan engine + pool, correlation, risk scoring,
 * and authentication. Built and installed independently of the web application
 * (nexpose-root) via {@code mvn clean install}.
 */
package com.rapid7.nexpose.console;
